# json-field-patcher
Lightweight Java utility to read and write nested JSON fields by dot-notation path-built for systems that store JSON documents in relational database columns without schema migrations.
